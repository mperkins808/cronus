# run this from nextjs (cronus) directory
run:
	rm ./.env.development && \
	export DEFAULT_CONFIG=./defaults/defaults.ini && \
	export CONFIG_PATH=./defaults && \
	export CUSTOM_CONFIG=../config/private.ini && \
	export OUTPUT_FILE=./.env.development && \
	export PORT=4001 && \
	npm run dev

